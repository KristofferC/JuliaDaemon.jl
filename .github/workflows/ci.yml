name: CI
on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: julia ${{ matrix.version }} / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    defaults:
      run:
        shell: bash
    # Nightly is informational: its JIT/stdlib churn should not gate CI.
    continue-on-error: ${{ matrix.version == 'nightly' }}
    strategy:
      fail-fast: false
      matrix:
        version: ['1.12', 'pre']
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - version: 'nightly'
            os: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
      - uses: julia-actions/cache@v2
      - name: End-to-end tests
        run: test/e2e.sh
        timeout-minutes: 25
        env:
          JLD_E2E_TRACE: ${{ runner.os == 'Windows' && '1' || '' }}
          JLD_DEBUG: ${{ runner.os == 'Windows' && '1' || '' }}
      - name: Remote REPL tests (pty)
        run: julia --color=yes test/repl.jl
