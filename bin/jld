#!/usr/bin/env bash
set -euo pipefail
SELF="$(readlink -f "${BASH_SOURCE[0]}")"
JLD_HOME="$(dirname "$(dirname "$SELF")")"
exec "${JLD_CLIENT_JULIA:-julia}" --startup-file=no --history-file=no --compile=min -O0 \
    "$JLD_HOME/src/client.jl" "$@"
